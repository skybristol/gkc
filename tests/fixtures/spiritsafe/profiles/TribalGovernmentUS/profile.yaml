name: TribalGovernmentUS
description: Test fixture for linkage parsing.

statements:
  - id: instance_of
    label: Instance of
    io_map:
      - to: https://www.wikidata.org/entity/P31
    type: statement
    required: true
    value:
      type: item
      fixed: Q7840353

  - id: office_held_by_head_of_state
    label: Office held by head of state
    io_map:
      - to: https://www.wikidata.org/entity/P162
    type: statement
    required: false
    max_count: 1
    guidance: Link to the office itself, not the person occupying it.
    entity_profile: OfficeHeldByHeadOfState
    linkage:
      target_profile: OfficeHeldByHeadOfState
      relationship:
        type: office_of_head_of_state
        direction: bidirectional
        reverse_statement_hint: applies_to_jurisdiction
      cardinality:
        min: 0
        max: 1
      workflow_policy:
        create: allowed
        select_existing: allowed
      traversal:
        max_depth: 1
    value:
      type: item
